{
    "name": "fixofy-root",
    "version": "1.0.0",
    "description": "Root for Fixofy Monorepo",
    "scripts": {
        "start": "cd server && node index.js",
        "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}